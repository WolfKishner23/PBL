# The URL of your local Node.js backend
VITE_API_URL=http://localhost:5000

# Optional: Direct URL to AI service (if needed)
VITE_AI_URL=http://localhost:8000
# IMPORTANT: Both you and your friend now share the same Neon Cloud database.
# Ensure your backend .env matches the credentials provided in the backend template.
